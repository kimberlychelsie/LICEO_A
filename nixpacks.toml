# Railway will use the Dockerfile if present (recommended).
# This file is kept as a fallback reference only.
[start]
cmd = "gunicorn app:app --bind 0.0.0.0:8000 --workers 2 --timeout 600 --graceful-timeout 120 --max-requests 2000 --max-requests-jitter 200"
